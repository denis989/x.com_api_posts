# Flask Application Settings
FLASK_SECRET_KEY="your_very_strong_and_random_secret_key_here"
# FLASK_ENV="development" # Optional: for development mode

# Twitter API Credentials (for OAuth 2.0 User Context or OAuth 1.0a)
TWITTER_CLIENT_ID="your_twitter_api_key_or_client_id"
TWITTER_CLIENT_SECRET="your_twitter_api_key_secret_or_client_secret"

# Optional: Twitter Bearer Token (for App-Only Authentication, if still used)
# TWITTER_BEARER_TOKEN="your_twitter_bearer_token"

# Google API Credentials (for OAuth 2.0)
GOOGLE_CLIENT_ID="your_google_oauth_client_id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your_google_oauth_client_secret"

# Celery Configuration (using Redis as an example)
CELERY_BROKER_URL="redis://localhost:6379/0"
CELERY_RESULT_BACKEND="redis://localhost:6379/0"

# Note: For production, these URLs might point to managed services.
# Ensure Redis is running and accessible if these defaults are used.
